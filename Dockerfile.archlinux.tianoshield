FROM devc-archlinux:base-devel

USER root

# specific setup (start) ======================

# specific setup (end) ======================

RUN pacman -Sy --noconfirm \
  bear \
  iasl \
  nasm \
  qemu-desktop

USER ${USER}
