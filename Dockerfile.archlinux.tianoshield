FROM devc-archlinux:base-devel

USER root

# specific setup (start) ======================

# specific setup (end) ======================

RUN pacman -Sy --noconfirm \
  bear \
  iasl \
  llvm \
  nasm \
  qemu-desktop \
  uncrustify

USER ${USER}
